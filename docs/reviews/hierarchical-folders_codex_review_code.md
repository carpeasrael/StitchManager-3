src-tauri/src/commands/folders.rs:69, severity: warning, description: `query_row(...).unwrap_or(0)` suppresses database/query errors when computing sibling `max_order`; this can silently continue with incorrect ordering instead of returning an `AppError`.
src-tauri/src/commands/folders.rs:368, severity: warning, description: `query_row(...).unwrap_or(0)` in `move_folder` also suppresses DB failures when computing destination sibling `max_order`, masking real errors and potentially assigning wrong `sort_order`.
src/components/Sidebar.ts:6, severity: warning, description: This file imports `./FolderMoveDialog` and `../utils/tree`, but both source files are currently untracked in git (`src/components/FolderMoveDialog.ts`, `src/utils/tree.ts`); committing only the tracked diff will break TypeScript compilation in clean checkouts.
