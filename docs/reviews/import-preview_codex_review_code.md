src/main.ts:1147 | warning | For drag-drop preview of 4+ files, only the parent directory of the first dropped file is scanned. If the drop contains files from different directories, files outside that first directory are silently excluded from preview/import.
src/main.ts:1151 | warning | `droppedSet` uses raw dropped paths while `scan_only` returns filesystem paths from Rust; separator/canonicalization differences (especially Windows `\\` vs `/`) can cause false negatives so dropped files are shown as “not found” even though they were scanned.
src/components/ImportPreviewDialog.ts:347 | warning | The `__new__` folder option is always shown, but when the dialog is opened without `sourcePath` (e.g. folder scan flow), selecting it always fails with “Kein Quellpfad...”. The option should be hidden/disabled unless `sourcePath` is available.
src-tauri/src/commands/scanner.rs:1252 | info | `test_scan_only_mixed_types_and_already_imported` does not call the `scan_only` command; it reimplements scan logic inline, so command wiring/serialization/error paths are not covered.
src-tauri/src/commands/scanner.rs:1333 | info | `test_import_files_with_bulk_metadata` does not call `import_files`; it manually executes SQL and mirrors logic, so command-level behavior (argument mapping, validation path, transaction/error behavior) is not actually tested.
