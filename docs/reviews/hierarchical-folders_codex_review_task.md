- Backend test coverage does not match the approved approach in `docs/analysis/20260319_2_hierarchical-folders.md` Step 14: the added `test_move_folder_*` and `test_recursive_file_count` tests mostly execute raw SQL that mirrors logic instead of invoking `move_folder` / `get_all_folder_file_counts` command functions, so command-level behavior (including no-op handling and error-type discrimination paths) is not fully verified.
