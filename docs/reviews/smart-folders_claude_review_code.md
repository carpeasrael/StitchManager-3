src/components/FileList.ts:106 | critical | `loadMoreFiles()` does not apply smart folder filters. Unlike `loadFiles()` (line 71-84), the infinite-scroll handler reads `searchParams` directly from AppState without merging the active smart folder's `filterJson`, and passes the raw `folderId` instead of `null` when a smart folder is selected. Pages 2+ will return results that don't match the smart folder's filter criteria, producing inconsistent data as the user scrolls.

src/components/Sidebar.ts:206-216 | critical | Regular folder click handler does not clear `selectedSmartFolderId`. When a user selects a smart folder (which sets `selectedSmartFolderId` and clears `selectedFolderId`), then clicks a regular folder, `selectedSmartFolderId` remains set. In `FileList.loadFiles()`, the non-null `selectedSmartFolderId` takes precedence: the smart folder filter is still applied and `folderId` is overridden to `null`. The regular folder's files are never shown. The reverse direction (smart folder click clearing `selectedFolderId`) is correctly implemented at line 472-476.

src-tauri/src/commands/smart_folders.rs:95-115 | warning | `update_smart_folder` executes up to three separate UPDATE statements (name, icon, filter_json) without wrapping them in a transaction. If an intermediate statement fails (e.g., icon UPDATE errors), the smart folder is left in a partially updated state. This is inconsistent with `update_smart_folder_sort_orders` (line 146) which correctly uses `unchecked_transaction()`.

src-tauri/src/commands/statistics.rs:53 | info | `.filter_map(|r| r.ok())` on `files_by_type` query results (and similarly at lines 90 and 128 for `top_folders` and `storage_by_folder`) silently drops rows that fail to deserialize. While this prevents panics, it can mask data integrity issues. Consider logging dropped rows or propagating the error.
