src-tauri/src/commands/scanner.rs:291 | warning | Rating validation in `import_files` occurs after file inserts have already been executed within the transaction. If the rating is out of range, the entire transaction rolls back — discarding all successfully inserted files. Move the `bulk_metadata.rating` range check before the insert loop (before line 255) to fail fast, avoid wasted pre-parsing I/O, and prevent a confusing UX where zero files are imported due to a metadata validation error.

src/main.ts:1146 | warning | Drag-and-drop preview for 4+ files derives the scan directory from only the first dropped file's parent (`parentDir`). When files originate from different directories, `scanOnly(parentDir)` will not find files outside that subtree. The subsequent `droppedSet.has(f.filepath)` filter silently excludes those files from the preview, making them unimportable. Consider computing the common ancestor of all dropped paths, or falling back to constructing `ScannedFileInfo` objects client-side (as the catch block already does) when paths span multiple directories.

src-tauri/src/commands/scanner.rs:508 | info | In `scan_only`, the progress event emission (`total_scanned % 50 == 0`) is inside the `is_supported_file` gate, so it only fires when a supported file happens to align with the counter. In `scan_directory` (line 194), the equivalent check is outside the supported-file block and fires for every 50th file regardless of type. This inconsistency means `scan_only` progress events may be sparse or absent in directories with many unsupported files.

src/components/ImportPreviewDialog.ts:119 | info | Sort column headers are `<span>` elements with click handlers rather than `<button>` elements. They are not reachable via Tab navigation and do not respond to Enter/Space, reducing keyboard accessibility (WCAG 2.1 criterion 2.1.1).
