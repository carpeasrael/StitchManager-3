src/components/FileList.ts:109 | critical | `loadMoreFiles()` ignores active smart-folder filters (`selectedSmartFolderId` + parsed `filterJson`) and always uses raw `appState.searchParams`; with pagination, page 0 is smart-folder filtered but later pages are fetched with different criteria, causing mixed/incorrect result sets.
src/components/Sidebar.ts:120 | warning | Clicking "Alle Ordner" only clears `selectedFolderId` but leaves `selectedSmartFolderId` unchanged, so the smart-folder filter remains active and the UI cannot reliably return to an unfiltered all-folders view.
src/components/Sidebar.ts:206 | warning | Selecting a regular folder does not clear `selectedSmartFolderId`; because `FileList` prioritizes smart-folder filtering when `selectedSmartFolderId !== null`, folder selection can appear to work in the sidebar while the file query still uses the smart-folder filter.
src-tauri/src/commands/statistics.rs:53 | info | Multiple query pipelines use `.filter_map(|r| r.ok())` (also at lines 90 and 128), silently dropping row-level SQL/deserialize errors instead of surfacing them; this can hide data/DB issues and produce incomplete dashboard stats without signaling failure.
