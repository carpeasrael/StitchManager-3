src-tauri/src/commands/folders.rs:170 | warning | `update_folder_sort_orders` does not validate affected row count per `folder_id`; non-existent/deleted IDs are silently ignored and the transaction still commits, which can produce partial reorders without surfacing an error.
src/components/Sidebar.ts:294 | warning | `reorderFolder` can run concurrently (multiple drag/keyboard actions before prior request finishes), causing race conditions from stale `folders` snapshots and nondeterministic final order depending on request completion order.
src-tauri/src/commands/folders.rs:325 | info | `test_update_folder_sort_orders` does not call `update_folder_sort_orders`; it reimplements raw SQL updates directly, so command-level behavior (argument mapping, command invocation path, and error handling) is not actually tested.
